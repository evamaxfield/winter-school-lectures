[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "rescale"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "numpy>=1,<2",
]

[project.optional-dependencies]
dev = [
    "ipython",
    "jupyterlab",
]